import React from 'react';

export const PWAInstallBanner: React.FC = () => {
  console.log('📱 PWAInstallBanner rendering with simplified implementation');
  
  return null; // Hidden for now to avoid hook issues
};