{
  "name": "harvox-tms",
  "type": "module",
  "scripts": {
    "clean": "rm -rf dist .vite node_modules/.vite node_modules bun.lockb",
    "fresh-install": "bun run clean && bun install",
    "fresh-dev": "bun run clean && bun run dev",
    "clear-cache": "rm -rf node_modules/.vite && rm -rf .vite"
  }
}